ul.item-selector.list-unstyled(data-current='0')
	- for (var i = 0; i < total; i++) {
		- if (i == 0)
			li.active.img-circle
		else
			li.img-circle
	- }
//
	-each page,i in pages
		- if (i == 0)
			li.pointer.active
				span.glyphicon(class="glyphicon-"+page.icon)
		else
			li.pointer
				span.glyphicon(class="glyphicon-"+page.icon)
